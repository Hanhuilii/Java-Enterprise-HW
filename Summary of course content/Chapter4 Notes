Chapter4
P95 、P59  container作用请求响应过程
P97、P99  servlet生命周期、生命周期方法（何时调用、作用、是否覆盖）
P98  servlet接口；GenericServlet类；HttpServlet类；MyServlet类
P104  servlet：Config\Context
P106、P121 、P126 ServletRequest/ServletResponse/HttpServletResponse/HttpServletRequest接口
P109其他HTTP方法
P110、p111、P13  get和post的区别
P114  HTTP幂等方法
P122 在请求中得到客户浏览器信息、cookie、session、HTTP方法、输入流
P123 InputStream/getHeader getIntHeader/getServletPort getLocalPort getRemotePort
P129 JAR
P130 常用MIME类型
P132 输出字符/字节 PrintWriter/OutputStream
P133 设置或增加响应首部Header
P136 servlet重定向：不想对请求做出响应
P138 重定向（客户端）和请求分派（服务器端）的区别 

Init\destroy只调用一次；init在调用service之前完成。 
每个请求都在单独线程运行，而且容器会分配一对新的请求和响应对象。
不覆盖方法：利用父类提供方法多少客户请求就有多少并发线程，只受容器资源限制。P100
容器·寻找类（找到servlet类文件，servlet生命开始）、加载类、初始化（构造函数运行-init-service（处理客户请求）-destroy）
每个web应用只有一个ServletContext. P104、P159
每个servlet都有一个ServletConfig对象
表单默认方法为get
HttpServletResponse\HttpServletRequest分别由ServletRequest\ServletResponse拓展
客户请求总是包含一个特定的HTTP方法。P108
HTTP GET方法和servlet中doGet方法有区别。P116
单个参数可以有多个值，需要返回数组的getParameter，而非返回String的getParameter。P121
HttpServlet扩展了javax.servlet.GenericServlet抽象类。P124
应使用JSP而非servlet把HTML发回到输出流，也可以用servlet处理输出流。P127
先设置内容类型再写入输出流。P130
不能在写到响应以后再调用sendRedirect; sendRedirect取一个String而不是URL对象
要得到二进制流，需要在响应上调用getOutputStream，请求体包含要处理文本或二进制内容，用getReader或getInputStream P123
