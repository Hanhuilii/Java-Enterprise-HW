yum -y install pcre-devel zlib-devel openssl-devel
groupadd nginx
useradd -g nginx -s /bin/false ngxin
tar xzf nginx-*.tar.gz -C /usr/src
cd /usr/src/nginx-*
./configure --prefix=/usr/local/nginx
--user=ngixn                 //指定运行的用户
--group=nginx                //指定运行的组
--with-file-aio               //启动文件修改支持
--with-http_stub_status_module   //启动状态统计
--with-http_gzip_static_module   //启动gzip静态压缩
--with-http_flv_module           //启用flv模块，提供寻求内存使用基于时间的偏移量文件
--with-http_ssl_module           //启用ssl模块

make && make install

•Configure the nginx.conf file

In the HTTP {... . } add the following code to set the load balanced server group. The weight parameter represents the weight. 
The greater the weight value, the greater the probability of being assigned to it.

upstream tomcat_server {
        server 192.168.1.1:8080 weight=1
        server 192.168.1.2:8080 weight=1
        }
        
•In the HTTP {... . Server {in}... } location / {... } Add a row of arguments

proxy_pass http://tomcat_server; 

Note: The default site of Nginx is proxy_pass on the configured tomcat_server load balancing server group.
